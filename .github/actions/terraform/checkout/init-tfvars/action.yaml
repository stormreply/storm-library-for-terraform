name: Set init.tfvars

runs:
  using: composite
  steps:

    - name: set init.tfvars
      shell: bash
      run: |
        # set/init-tfvars
        cat << EOF >> init.tfvars
        backend_bucket = "${{ env.BACKEND_BUCKET }}"
        oidc_principal = "${{ env.OIDC_PRINCIPAL }}"
        EOF
        echo "init.tfvars:"
        cat init.tfvars
